# Offline public demo (default). No API key is required in this mode.
APP_MODE=demo

# Required only when APP_MODE=live. Keep the real value in an untracked .env file.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=your_anthropic_model_id_here
