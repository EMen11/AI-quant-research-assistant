# Evaluation tests

This directory is reserved for versioned evaluation cases introduced in later speedrun blocks.
Block 1 deliberately contains only deterministic unit and startup integration tests.
